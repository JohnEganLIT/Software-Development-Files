#include <iostream>

using namespace std;

int main()
{
    first + second = total;

    if(first == second)
    {
        cout << "Values are the same\n";
        cout << "Total is double of either value";
    }

    else
    {
        cout << "Values are not the same\n";
        cout << "Total is less than double of the larger value\n";
    }
}
